import Loading from "./loading";

export default function LoadingPage() {
  return (
    <div className=" flex flex-1 items-center justify-center">
      <Loading className="text-6xl" dotSize="1rem" />
    </div>
  );
}
